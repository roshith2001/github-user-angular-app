import { PasswordSimilarityDirective } from './password-similarity.directive';

describe('PasswordSimilarityDirective', () => {
  it('should create an instance', () => {
    const directive = new PasswordSimilarityDirective();
    expect(directive).toBeTruthy();
  });
});
